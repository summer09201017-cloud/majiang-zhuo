# CLAUDE.md — 麻將桌(majiang-zhuo)

給在這個 repo 裡動工的 AI。**動手前先讀這份與 `roadmap.md`、`讀我-HANDOFF.txt` 最新 ★段。**

## 現況(2026-09-02 · v0.6.0 · M0~M5 全部完成 · **已上線**)

線上 https://majiang-zhuo.pages.dev ·
repo https://github.com/summer09201017-cloud/majiang-zhuo(公開)

台灣 16 張麻將,一人 vs 三家電腦。純前端 PWA,**零相依、零建置、零美術檔、零後端**。

| 里程碑 | 內容 | 狀態 |
|---|---|---|
| **M0 骨架** | 144 張牌模型、種子洗牌、砌牌牆擲骰開門、發牌、補花、摸打、四家版面 | ✅ |
| **M1 純規則引擎** | 胡牌型分解、聽牌、吃碰槓合法性、v1 台數表(node 直跑 + golden cases) | ✅ |
| **M2 回合狀態機** | 吃碰槓胡**仲裁**、一炮多響(可切)、連莊、結算、⚙ 設定面板 | ✅ |
| **M3 AI 三級** | shanten + 有效進張 + 打點/避險、💡 兩層提示、量化驗收 | ✅ |
| **M4 手感** | 吃碰槓胡專屬音效、打牌飛入動畫、牌河依座位旋轉、倒牌、彩帶、🏆成績榜 | ✅ |
| **M5 上線** | 畫布寬度自適應、鎖橫向三件組、PNG 圖示、存檔續玩、CF Pages | ✅ 已上線 |

**⚠ 這不是教會遊戲系列的一員**(同 `zhipai-zhuo` 紙牌桌 / `dianzhu-tai` 彈珠檯慣例):
個人/家庭懷舊系,不進大廳、不進 sites.json、不接統計 beacon、不放經文、不做分齡。
**只計台/分,不做籌碼與下注。**

驗收:`node scripts/smoke.mjs`(純邏輯)+ `node scripts/verify-ui.mjs`(真瀏覽器;
`LIVE_URL=` 可驗線上、`SHOTS=<夾>` 另存截圖)。**項數以跑出來的為準,文件不寫死**
(寫死的數字隔一輪就變成謊話)。

## 核心約束(不可破)

- **零相依**:沒有 package.json、沒有建置。直接開 `index.html` 就能玩。
- **零美術檔**:牌面 42 種全程式畫;**零後端**:紀錄只在 localStorage(全包 try/catch)。
- **鎖橫向**(跟紙牌桌相反、跟彈珠檯相同)。16 張手牌 + 三家副露 + 牌河,直向塞不下
  ⇒ 直向一律蓋版請人轉橫。三件組:manifest 的 `orientation` + 直向蓋版 + 手勢中進全螢幕。
- **邏輯畫布:高度固定 540、寬度 960~1280 隨視窗比例伸縮**(M5)。
  ★ 這不是為了好看,是為了**手牌按得到**:縮放由高度決定,想把牌變大只能把畫布變寬。

## 一檔一責

```
src/config.js         全域設定與所有牌的尺寸。★ VERSION 改了,sw.js 的 CACHE 要一起 bump(smoke 在守)
src/tiles.js          144 張牌模型 + 種子洗牌 + 砌牌牆 + 守恆保險絲 conserve()
                      + ★唯一佈局函數 tilePos / tagRect(畫牌與點牌同一份 = 判定=畫面)
                      + 42 種牌面的程式畫法與 offscreen 快取(document 只在這裡碰)
src/rules/hu.js       胡牌型分解(34 格計數)、聽牌 waits、canWinWith。★ 只有一種胡型:5 面子 + 1 將
src/rules/shanten.js  向聽數(記憶化)、有效進張、bestShantenAfterDiscard
src/rules/meld.js     吃/碰/槓的「能不能」+ meldToSet(門清與暗刻的唯一判準)
src/rules/score.js    v1 台數表 + scoreHand(所有拆法取最高)+ taiText
src/table.js          ★唯一 apply 入口與回合狀態機:phase = play / react / win / washout / over
                      drawTile / playDiscard / beginReact / declare / resolveReact / doPon…doAddkan
                      doWin / doSelfWin / nextHand / stepAuto / humanActions
src/ai.js             三級電腦 + 💡兩層提示。★ 只用檯面看得到的資訊(leftCounts),不偷看手牌
src/game.js           控制器:rAF 迴圈、輸入語意、每步跑守恆保險絲 fuse()。頂層不碰 DOM
src/renderer.js       畫牌桌(只讀不改);按鈕框 push 進 renderer.btns
src/input.js          指標/鍵盤 → 邏輯座標,不懂麻將規則
src/sfx.js            零音檔音效(收割自紙牌桌)+ 吃/碰/槓/胡/自摸/摸牌 專屬音
src/confetti.js       彩帶:畫在同一張 canvas、從上方灑落、尊重 prefers-reduced-motion
scripts/smoke.mjs     純邏輯驗收(node 直跑)
scripts/verify-ui.mjs 真瀏覽器(playwright 自己找;找不到回 3 = 跳過,不靜靜算通過)
scripts/balance.mjs   量化驗收:三級電腦對打,比胡牌率/放槍率/收場向聽(smoke 不跑,太慢)
scripts/make-icons.mjs 從 icon.svg 產 PNG 圖示(只有改了 icon.svg 才要跑;產物進 git)
```

## ★ 動了就會壞的

1. **守恆保險絲**(`conserve` + `game.fuse()`):牌牆+四家手牌+剛摸+副露+牌河+花 ≡ 144、
   id 零重複。**麻將 bug 九成是「牌憑空多/少」,而且是靜默的** —— 拿掉它就回到考古時代。
   0901 實錄:它抓到的第一個破口是 smoke 自己(`drawn[0] = null` 把莊家的牌丟進虛空)。
2. **`tilePos` 是唯一佈局函數**:renderer 畫牌、input 點牌、smoke 驗版面全吃它。
   `handHit` 也走它 —— 兩邊各算一套就是「點不到牌」的來源。
3. **副露壓縮的分母要扣掉一張牌寬**(`tilePos` 的 `k`):縮的是間距不是牌,
   `(AVAIL - M.W) / (need - M.W)`。寫成 `AVAIL / need` 末端會凸出 `MELD_RIGHT` 十幾 px。
4. **`playerDiscard` 自己會換人**:外面再 `G.turn = nextSeat(0)` 一次 = **永遠跳過下家**。
   smoke 有「回合逆時針 0→1→2→3 不跳過」與「四家丟牌數相差 ≤1」兩條在守。
5. **版面座標是量出來的不是喬出來的**:`LAY` 裡每個數字都被 smoke 的
   「最擠的牌桌:23 塊區域兩兩不重疊」釘著(含左上角兩顆鈕)。要動先跑 smoke。
   0901 兩次退件都是這類:左家花牌壓在自家手牌底下、又壓掉音效鈕一半。
6. **`const` 宣告不會掛上 `window`**:Playwright 驗收要用 `window.__game` / `window.__renderer`
   (index.html 末尾曝出來的),而且要等 `__renderer.btns.length >= 2` 才算「畫過一幀」——
   `state` 在 boot 當下就成立、rAF 還沒畫,驗太早是驗法的錯不是遊戲的錯。
7. **牌面一定要走 offscreen 快取**(`face()` / `warmFaces()`):每幀重畫 144 張的筒圈與竹節
   會直接把手機拖垮。
8. **`msgBar` 一律畫下方**:畫上面會壓住對家的手牌與副露(紙牌桌 0831 兩次退件的老帳)。
9. **台數表加一項 = 三個地方**:`score.js` 的 `TAI` 表 + `scoreOne` 裡的判定 +
   smoke 的一個 golden case。★ smoke 有一條「台數表 27 條全部都有案例涵蓋」在守 ——
   加了表卻沒案例、或判定條件寫錯永遠不成立,都是**靜默的死碼**,只有這條抓得到。
10. **v1 刻意拿掉的台不可以偷偷長回來**(平胡/獨聽/單吊/邊張/嵌張/七對子):
   它們需要「回推聽牌形狀」,是唯一逼你另寫一套邏輯的東西。smoke 有一條在守。
11. **`scoreHand` 每一輪要重建副露的 set 物件**:裡面會就地改 `open`
   (食胡補成的刻子算明刻),共用同一份會汙染下一種拆法。
12. **property test 一定要有「樣本非空」的守門**:0901 第一版用隨機 17 張跑 4000 手,
   **0 手成胡**,四條 property 全是空跑假綠。要用**構造法**生保證成胡的手牌。
13. **仲裁一定要「先收齊、再一次判」**(`beginReact` → `declare` → `resolveReact`):
   邊問邊執行 = 先宣告的人先贏,優先權整個歪掉。`resolveReact` 是唯一出口。
14. **搶槓那張牌被 `pendingKan` 抱著,不在任何一個「牌的容器」裡**:胡成了要把它放進
   加槓者的牌河,沒胡成才 `completeAddkan`。忘了就守恆少一張(0901 當場被保險絲抓到)。
15. **槓之前一定要 `mergeDrawn`**:槓完會 `drawTile` 補一張,那一行直接覆蓋 `G.drawn` ——
   沒先把原本剛摸的那張併回手牌,它就憑空消失。三個槓(明/暗/加)都要。
16. **碰/吃之後 `mustDiscard = true`**:那一家直接打牌、**不再摸一張**。
   驅動迴圈判「要不要摸牌」看的就是這個旗標。
17. **`humanActions` 是玩家介面的唯一真相**:renderer 照它畫鈕、`game.onBtn` 照它執行 ——
   畫得出來的鈕就一定按得動,反之亦然。動作列的框也只有一份(`actBarLayout`)。
18. **`opts.hands` 是「總共打幾局」,連莊也算一局**:用「換莊次數」當上限的話,
   莊家一直連莊就永遠打不完(流局也連莊,弱 AI 很容易連十幾把)。
19. **無頭 Chromium 會把 rAF 節流掉**:驗收點完按鈕要自己 `__renderer.draw(__game)` 推一幀,
   否則讀到的 `renderer.btns` 是上一幀的,會誤判成「鈕不見了」。
20. **手牌張數有兩種型態:3N+1(輪不到我)與 3N+2(剛摸完)**。算錯型態向聽數整個歪掉,
   而且是**看起來合理的垃圾**。`shantenOfForm` 一律用**實際張數**判斷,不要用「現在輪到誰」去猜;
   張數兩種都不是就回 99(明顯壞),別靜靜給答案。
21. **`shanten` 是純函式 + 記憶化**:別在裡面塞任何跟 `G` 有關的東西,否則快取會給出過期答案。
   一次 AI 決策會問它上百次(有效進張要試 34 張、提示還要往前多看一步)——
   沒有這層快取,提示要 590ms;有了 ~150ms。
22. **AI 不可以偷看別人的手牌**:所有判斷只能走 `leftCounts`(自己手牌 + 四家副露 + 四家牌河)。
   要加「別人是不是聽牌了」的推測,也只能從牌河推,不可以讀 `G.hands[other]`。
23. **💡 提示絕不可以是純貪心**(skill `solitaire-solver-kit` 第〇節):
   兩層 —— 先挑「向聽數最好、有效進張最寬」,前 2 名再往前看一步(摸到有效張之後平均還能多寬)。
   只看第一層會被「現在最寬、下一步卡死」的牌騙走。
24. **難度旋鈕一定要量化驗收**(`scripts/balance.mjs`),而且**只斷言真的存在的差距**。
   實測:新手 vs 另外兩級差距很穩;**老手 vs 普通在點數上打平**(見 ai.js 開頭的誠實記帳)。
   硬斷言一個不存在的差距,只會做出一條會亂閃的測試 —— 那比沒有測試更糟。
25. **「💡 提示」在自己回合也會進 `humanActions`**:任何拿「acts 非空」當「玩家要做決定」
   判斷的地方都要改成看 `phase === 'react'`(0901 verify-ui 就這樣紅了一條)。
26. **驗收時不可以用 `river[N].length` 當「牌有沒有打出去」的證據**:被人碰/吃/槓走之後
   `takeFromRiver` 會把它拿掉,長度變回原值 —— 看起來像「點了沒反應」。用 `G.discards`。
27. **驗收的快轉迴圈要每圈重讀 `__game.G`**:`onBtn('next')` 打完整場會 `newMatch()`,
   整個 `G` 物件被換掉;抓著舊參照會在已結束的牌局上空轉。牌局中途結束也要按「下一局」繼續。
28. **牌河依座位旋轉:±90 的格子寬高要互換、180 度不要換**。`tilePos` 一處算好,
   `renderer.img()` 裡的 `swap` 只認 ±90 —— 把 180 也一起換,對家的牌會被畫成壓扁的。
29. **倒牌的張數是「17 + 槓數」,不是固定 17**:一副槓佔 4 張牌卻只算一副面子。
   (0902 實錄:verify-ui 十次紅一次報 18 張,是斷言算錯不是產品錯。)
   同理,**槓完 need 少一副** ⇒ 手牌從 16 變 13,寫測試夾具時張數要跟著改。
30. **彩帶從上方灑落,不要從中心爆開**:結算面板就在畫面正中央,爆開會蓋住台數與分數
   (skill `win-confetti` 的老規矩)。
31. **動畫是純觀感,不可以參與任何判定**:`game.fly` 只是把「已經打進牌河的那張」
   補一段位移,規則早就跑完了;renderer 在飛的期間跳過那一格不畫,如此而已。
   `prefers-reduced-motion` 要整個關掉(不飛牌、不放彩帶)。
32. **`localStorage` 讀回來一定要驗型別**:使用者的瀏覽器裡什麼都可能有(手動改過、
   舊版寫的、根本不是 JSON)。`readRuns` 對這三種都有測試釘著。
33. **版面寬度是算出來的,不是寫死的**:`layoutFor(W)` 一處算好,`LAY` 是 **`let`**
   (`setLayoutWidth` 會換掉整個物件)。★ 因此 smoke 匯出 `LAY` 一定要用 **getter**,
   不然拿到的是匯出當下的快照,測起來永遠是 960。
34. **左右家的副露是縱向排的,吃的是畫布高度**:不可以跟橫向的可用寬度共用預算
   (`SIDE_MELD_AVAIL` 另外算)。畫布一變寬就會排到畫布外、還會壓到自家手牌。
35. **版面測試要在好幾個寬度下各驗一次**(960/1024/1170/1280):只驗 960 等於沒驗到自適應。
36. **存檔一律存牌的 id,讀回來用 `makeTiles()` 重建**,而且**讀回來要跑守恆保險絲** ——
   存檔可能是舊版寫的、被手改過、或根本不是我們寫的。湊不出 144 張就整份丟掉。
37. **結算畫面離開時 `live` 存 null**:比分留著、下次回來從下一局開始。
38. **iOS 主畫面圖示不吃 SVG**:一定要 PNG + `apple-touch-icon`,否則加到主畫面
   會變成一張網頁縮圖(整個艦隊都踩過)。
39. **全螢幕/鎖橫向一定要在使用者手勢裡呼叫,而且全包 try/catch**:
   桌機與 iOS 都會拒絕,被拒絕不可以讓遊戲壞掉。只在觸控裝置自動試一次。
40. **手牌寬由 `LAY.HAND_W` 決定**(隨畫布寬度變),不要再讀 `CONFIG.TILE.HAND.W`:17 張要排進 960 邏輯寬。手機橫向實體寬因此只有 ~41px(<44 觸控門檻),
   在 config 裡再加寬只會把牌擠出畫布 —— 真正的解是 M5 的「邏輯畫布寬度自適應」。

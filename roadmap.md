# roadmap.md — 麻將桌(majiang-zhuo)

台灣 16 張麻將，一人 vs 三家 AI。純前端 PWA、**零相依、零建置、零美術檔、零後端**。

**定位：個人/家庭懷舊系**（同 `zhipai-zhuo` 紙牌桌 / `dianzhu-tai` 彈珠檯）——
不進教會大廳、不進 sites.json、不接統計 beacon、不放經文、不做分齡。
**只計台/分，不做籌碼與下注**（沿撿紅點慣例）。

---

## 一、已拍板（2026-09-01）

| 項目 | 決定 | 理由 |
|---|---|---|
| 規則流派 | **台灣 16 張**，144 張（含花牌 8） | 台灣人最熟，家人拿到就會玩；國標 81 番表工作量是港式 5 倍且沒人玩 |
| 放哪 | **獨立 repo**，收割紙牌桌的殼 | `cards.js` 是 52 張撲克專用；硬塞會把共用層扭曲、風險轉嫁給 4 張已上線牌桌 |
| 裝置 | 手機 + 電腦都要 | 同一份邏輯畫布 + 自適應重排（照 `<id>Layout` 作法） |
| 方向 | **鎖橫向**（跟紙牌桌相反，跟彈珠檯相同） | 16 張手牌 + 三家副露 + 牌河，直向 540 寬塞不下 |
| 金錢 | 只計台/分，無籌碼、無下注 | 家庭懷舊系定位 |

## 二、待拍板（都有預設值，不擋開工）

| 題目 | 預設 | 何時要決定 |
|---|---|---|
| ~~一炮多響 vs 順位優先~~ | ✅ 已定案：**一炮多響**，⚙ 面板可切成「順位優先」 | 2026-09-01 |
| ~~局數~~ | ✅ 已定案：單局 / **一圈(4局)** / 全將(16局)，⚙ 可切。**連莊也算一局** | 2026-09-01 |
| 過水、詐胡罰則 | v1 不做 | M4 再議 |
| 台數表細項（見 §6 草案） | 依 §6，開工前給你勾 | M1 |

---

## 三、檔案配置（一檔一責）

```
src/config.js         VERSION ★ 改了 sw.js 的 CACHE 要一起 bump（smoke 在守）
src/tiles.js          144 張牌模型 + 種子洗牌 + 牌面 offscreen 快取
                      + tilePos ★唯一佈局函數：畫牌與點牌同一份 = 判定=畫面
src/rules/hu.js       胡牌型分解（純函式，node 直跑）
src/rules/shanten.js  向聽數 + 有效進張（AI 大腦 & 💡提示的來源）
src/rules/meld.js     吃/碰/槓合法性
src/rules/score.js    台數計算（純函式 + golden cases）
src/table.js          回合狀態機 + 吃碰槓胡仲裁 ★唯一 apply 入口，UI 與 AI 同吃
src/ai.js             三級 AI（新手／普通／老手）
src/renderer.js       畫牌桌，只讀不改；按鈕框 push 進 renderer.btns
src/input.js          指標/鍵盤 → 邏輯座標，不懂規則
scripts/smoke.mjs     純邏輯驗收（node 直跑）
scripts/verify-ui.mjs 真瀏覽器（playwright 自己找）
```

### 從 zhipai-zhuo 收割
- **整檔搬**：`input.js`、`sfx.js`、`confetti.js`、`config.js` 骨架、`sw.js` 慣例、
  `smoke.mjs` / `verify-ui.mjs` 骨架、手機尺寸換算表
- **只留殼**：`renderer.js` 的綠呢桌面 / `btns` 命中框 / `msgBar(...,'bottom')`
- **不能用**：`cards.js`（52 張撲克模型與 `cardPos` 是為單人接龍寫的）

---

## 四、里程碑

| 階段 | 產出 | 驗收 |
|---|---|---|
| **M0 骨架** ✅ | 144 張牌模型、種子洗牌、砌牌牆、擲骰開門、發牌、補花、摸打；四家版面 | `smoke` 47/47 + `verify-ui` 12/12（2026-09-01） |
| **M1 純規則引擎** ✅ | 胡牌型分解、聽牌、吃碰槓合法性、v1 台數表（**完全不碰 DOM，node 直跑**） | `smoke` 99/99 + `verify-ui` 14/14（2026-09-01） |
| **M2 回合狀態機** ✅ | 吃碰槓胡仲裁、一炮多響（可切）、連莊、結算、⚙ 設定 | `smoke` 169/169 + `verify-ui` 23/23（2026-09-01） |
| **M3 AI 三級** ✅ | shanten（記憶化）+ 有效進張 + 打點/避險、💡 兩層提示 | `smoke` 196/196 + `verify-ui` 29/29 + `balance.mjs`（2026-09-02） |
| **M4 手感** ✅ | 吃碰槓胡專屬音效、打牌飛入動畫、牌河依座位旋轉、倒牌、彩帶、🏆成績榜 | `smoke` 221/221 + `verify-ui` 33/33（2026-09-02） |
| **M5 上線** | 畫布寬度自適應、鎖橫向三件組、PNG 圖示、存檔續玩 | ✅ `smoke` 248/248 + `verify-ui` 39/39（2026-09-02）**還沒部署** |

### ✅ 那筆帳還掉了（2026-09-02）

**手機橫向手牌 46.6 實體 px**（iPhone 852×393：邏輯寬 1171 × scale 0.728 × 牌寬 64），
過了 44 的觸控門檻，而且**寬螢幕不再留黑邊**。做法就是下面原本規劃的那個：
邏輯高度固定 540、寬度隨視窗比例伸縮（960~1280），多出來的寬度全給手牌。
`LAY` 從寫死的絕對座標改成 `layoutFor(W)` 算出來的，版面測試也改成
**在 960 / 1024 / 1170 / 1280 四個寬度下各驗一次**（只驗 960 等於沒驗到自適應）。

以下是當初的分析，留著當紀錄：

### ★ M0 量到的、留給 M5 的一筆帳：手機橫向手牌只有 ~41 實體 px

17 張手牌被「960 邏輯寬」鎖死，牌寬 52 已是上限（span 928，兩邊各留 16）。
手機橫向的 scale 由**高度**決定（932×430 ⇒ 0.796；852×393 ⇒ 0.728），所以實體寬只有 38~41px，
低於 44 的觸控門檻；在 config 裡再加寬只會把牌擠出畫布。

**真正的解**：寬螢幕不要 letterbox —— 邏輯高度固定 540、寬度隨視窗比例伸縮（960~1200），
多出來的寬度全給手牌。`LAY` 目前是對 960 寫死的絕對座標，要一併改成相對式。
⚠ 別在 M2/M3 之前做：吃碰槓胡的按鈕還沒進版面，現在改等於改兩次。
`verify-ui` 有一條盯著這個數字（門檻 40，守的是「不要再退步」，不是「已經達標」）。

M1 + M2 是真正的骨頭，其餘大多是收割既有套件。

---

## 五、★ 保命規則（動了就會壞）

1. **守恆保險絲**：每次 `apply` 後驗
   `牌牆 + 四家手牌 + 四家副露 + 牌河 + 花牌區 ≡ 144`。
   麻將 bug 九成是「牌憑空多／少」，這條在第一時間抓到，不必事後考古。
2. **種子洗牌**：沒有種子，麻將 bug 完全無法重現。沿紙牌桌慣例。
3. **判定=畫面**：牌的座標只有一份 `tilePos`，畫牌與點牌同吃它。
4. **UI 手勢與 AI 走同一份 `apply`**：各寫一份遲早分岔（紙牌桌鐵則）。

## 六、★ 已知地雷（開工前先讀）

- **💡提示絕不可以是純貪心** — `solitaire-solver-kit` 第〇節，0831 新接龍、0901 蜘蛛接龍
  已經犯過兩次。麻將的提示要算「打這張後 shanten 是否下降 × 有效進張種類 ×
  場上還剩幾張（扣自己手牌 + 全部副露 + 全部牌河）」，不是「打最沒用那張」。
- **牌面 42 種開機時各畫一次進 offscreen canvas**，之後每幀只 `drawImage`。
  不要每幀重畫 144 張的筒圈與竹節。
- **`msgBar` 一律畫下方** — 上方會壓住對家副露。
- **verify-ui 驗畫面前要等第一幀**（`__renderer.btns.length >= N`），
  `state==='title'` 在 boot 當下就成立但 rAF 還沒畫。
- **手機觸控換算**：scale≈0.7 ⇒ 要 44 **實體** px，邏輯得寫 **63**。
- **存檔續玩**：一將 16 局約一小時，手機一定會被切走。整局狀態存 localStorage，
  **全包 try/catch**（Safari 私密模式）。
- **突變測試還原不要用 `git checkout`** —— 存副本再寫回。

### M0 現場新增的地雷（2026-09-01；都已寫進 CLAUDE.md ★段，並各補了測試）

- **`playerDiscard` 自己會換人**，外面再推一次 = 永遠跳過下家。整局照樣守恆、照樣流局，
  測試全綠也看不出來 —— 靠「回合逆時針不跳過」與「四家丟牌數相差 ≤1」兩條才抓到。
- **副露壓縮的分母要扣掉一張牌寬**：`(AVAIL - W) / (need - W)`，不是 `AVAIL / need`，
  否則末端會凸出 `MELD_RIGHT` 十幾 px。
- **版面座標一定要有「兩兩不重疊」的測試**：第一版左家花牌壓在自家手牌底下、又壓掉音效鈕一半，
  兩次都是看截圖才發現。現在 23 塊區域（含左上角兩顆鈕）由 smoke 釘著。
- **`const` 不會掛上 `window`**：Playwright 等第一幀要用 `window.__renderer.btns.length`
  （index.html 末尾有曝 `__game` / `__renderer`）。

### M5 現場新增的地雷（2026-09-02）

- **`LAY` 從 `const` 變成 `let`（`setLayoutWidth` 會換掉整個物件）⇒ smoke 匯出它一定要用 getter**。
  用 `{ LAY }` 匯出的是**匯出當下的快照**，換了寬度也拿不到新的，四個寬度全都測成 960。
- **左右家的副露是縱向排的，吃的是畫布高度**，不可以跟橫向的可用寬度共用同一個預算。
  畫布一變寬，左右家的副露就排到畫布外、還會壓到自家手牌（`SIDE_MELD_AVAIL` 另外算）。
- **存檔測試不可以「跑固定步數就存」**：真 AI 常常四十步內就有人胡了，
  存檔會變成「下一局」，比的是兩個不同的東西。要跑到「有副露、還在進行中」才存。
- **讀存檔一定要跑守恆保險絲**：湊不出 144 張就整份丟掉重開，不要帶著壞局繼續打。
- **iOS 主畫面圖示不吃 SVG** —— 一定要 PNG + `apple-touch-icon`。
- **全螢幕/鎖橫向要在使用者手勢裡呼叫、全包 try/catch**：桌機與 iOS 都會拒絕。

### M4 現場新增的地雷（2026-09-02）

- **倒牌的張數是「17 + 槓數」，不是固定 17**。一副槓佔 4 張牌卻只算一副面子。
  verify-ui 十次紅一次報 18 張——**是斷言算錯，不是產品錯**。
  同理，**槓完 `need` 少一副** ⇒ 手牌從 16 變 13，寫測試夾具時張數要跟著改
  （這條又踩了一次「張數給錯，向聽數就是垃圾」）。
- **牌河旋轉：±90 的格子寬高要互換，180 度不要換**。`renderer.img()` 的 `swap`
  只認 ±90；把 180 也一起換，對家的牌會被畫成壓扁的。
- **彩帶從上方灑落，不要從中心爆開**——結算面板就在正中央，爆開會蓋住台數與分數。
- **動畫不可以參與判定**：`game.fly` 只是把「已經進牌河的那張」補一段位移，
  renderer 在飛的期間跳過那一格不畫，如此而已。`prefers-reduced-motion` 整個關掉。
- **`localStorage` 讀回來一定要驗型別**：不是陣列、元素型別不對、根本不是 JSON——
  三種都要擋，三種都有測試釘著。

### ★ M3 量到的結果與一筆誠實記帳（2026-09-02，`node scripts/balance.mjs 80`）

座位 0 放待測的那一級，另外三家固定「普通」，三級跑同一批種子：

| 等級 | 胡牌率 | 放槍率 | 平均台數 | 平均得分 | 收場向聽 |
|---|---|---|---|---|---|
| 新手 | 8% | 23% | 3.2 | −9.1 | 1.91 |
| 普通 | 24% | 18% | 3.6 | +2.6 | 0.61 |
| 老手 | 28% | 23% | 3.1 | +1.1 | 0.50 |

- **新手 vs 另外兩級：差距很大且穩定**（樣本再大也在）。難度旋鈕確實接上了。
- **老手 vs 普通：老手「快」（向聽低、胡牌率高），但點數上打平。** 這是現況，不是 bug。
  根因：台灣麻將這一版**沒有振聽／過水**，「安全牌」沒有硬保證，統計上的危險度訊號很弱——
  避險權重調 240 / 190 / 110 三組都量過，放槍率都不降，只是把速度拖慢。
  要讓老手在**點數**上真的贏，需要「從別人的牌河反推聽牌型」＋「推/收的期望值計算」，
  那是另一個量級的工程，不是調權重。留給以後。
- 因此 `balance.mjs` **只斷言真的存在的差距**（新手 vs 另外兩級）。
  硬斷言一個不存在的差距，只會做出一條會亂閃的測試——那比沒有測試更糟。

### M3 現場新增的地雷（2026-09-02）

- **手牌張數有兩種型態（3N+1 / 3N+2）**，算錯型態向聽數會給出**看起來合理的垃圾**。
  `shantenOfForm` 一律用實際張數判斷；兩種都不是就回 99。
  （M3 兩條測試紅燈都是夾具給錯張數：一個 15 張、一個 17 張，`setupTable` 只補不刪。）
- **測試夾具只補不刪**：`setupTable` 原本只在張數不足時補牌，多了不會修剪。已修成兩邊都管。
- **`shanten` 一定要記憶化**：一次決策問它上百次；沒快取提示要 590ms，有了 ~150ms。
  （純函式才敢快取——別在裡面碰 `G`。）
- **「💡 提示」在自己回合也會進 `humanActions`**：所有拿「acts 非空」當「玩家要做決定」
  判斷的地方都要改看 `phase === 'react'`。
- **smoke 會被真 AI 拖慢**：AI 批次從 30 局縮到 10 局（24 秒 → 10 秒），
  量化比較整個移到 `balance.mjs`。

#### 三個「測試會閃」的坑（都修掉了，連跑 8 次全綠）

會閃的測試比沒有測試更糟——它會訓練人忽略紅燈。這三個都花了不只一輪才定位：

1. **不可以用 `river[N].length` 當「牌有沒有打出去」的證據**。打出去之後若被人碰/吃/槓，
   `takeFromRiver` 會把它從牌河拿走，長度又變回原值，看起來就像「點了沒反應」。
   要用 `G.discards`（只增不減的計數器）。
   ★ 定位過程：診斷印出 `handHit=5 可打=true btnAt=-` 才確定是**斷言錯，不是遊戲錯**。
2. **快轉迴圈要每圈重讀 `__game.G`**。打完整場時 `onBtn('next')` 會 `newMatch()`，
   整個 `G` 物件被換掉；抓著舊參照的迴圈會在一個已經結束的牌局上空轉到 guard 用完。
3. **牌局可能在快轉途中就結束**（有人胡了／流局），要按「下一局」繼續找，
   否則永遠等不到「輪我打牌」。

另外記一筆**流程**上的教訓：補丁腳本「多塊 assert、最後才寫檔」時，
後面任一塊失敗會把前面已成功的**整包丟掉**，而腳本已經印了 `ok`。
（hook `patch-atomic-guard` 早就警告過這件事，這一輪還是踩了。）
⇒ 一塊改完就寫一次檔，而且**寫完一定 grep 回頭對賬**，別信腳本自己印的 ok。

### M2 現場新增的地雷（2026-09-01）

- **搶槓那張牌會憑空消失**。加槓時牌被 `pendingKan` 抱著，不在任何一個「牌的容器」裡；
  搶槓胡成之後忘了把它放進牌河 ⇒ 143/144。守恆保險絲當場抓到。
- **槓完補摸會覆蓋 `G.drawn`**，把玩家原本剛摸的那張吃掉 ⇒ 143/144。三個槓都要先 `mergeDrawn`。
- **無頭 Chromium 會把 rAF 節流掉**：設定面板明明開著，`renderer.btns` 卻只有 `new/gear`，
  查了幾輪才發現是驗法的問題——點完要自己推一幀。（`canvas-playwright-verify` 早就寫過這一類。）
- **測試夾具要連花牌一起清**：開局補花隨機發到的花每張 1 台，不清掉台數的期望值就對不準。
- **`playDiscard` 之後不一定馬上換人**：有人能吃碰槓胡就停在 `react`。
  M0 寫的 `turn === 1` 斷言在 M2 全部要改。

### M1 現場新增的地雷（2026-09-01）

- **property test 一定要有「樣本非空」的守門**。第一版用「隨機抓 17 張」跑 4000 手驗不變量——
  **0 手成胡**，四條 property 全部空跑、全部假綠。隨機牌成胡的機率低到可以當 0，
  必須用**構造法**（隨機組 1 對將 + 5 副面子）生出保證成胡的手牌再驗。
- **台數表要有「每一條都被案例涵蓋」的檢查**。加了 `TAI` 表卻忘了寫案例、
  或判定條件寫錯永遠不成立，都是**靜默的死碼**。這條檢查一上線就抓出五條沒案例的
  （小三元、小四喜、大四喜、地胡、河底撈魚）與一個沒進涵蓋清單的（清一色）。
- **`scoreHand` 每一輪要重建副露的 set 物件**：裡面會就地改 `open`（食胡補成的刻子算明刻），
  共用同一份會汙染下一種拆法。
- **寫測試案例時小心「順手中了別的台」**：小三元的案例原本三副順子全用萬子，
  結果連混一色一起中（11 台而不是 7 台），小三元就被蓋掉看不出來了。順子要刻意跨兩門。
- **`123p 456p 7p` 不是單吊七筒，是三面聽 1/4/7 筒**（測試寫錯、程式是對的）。

---

## 七、台灣 16 張規則備忘（實作用）

**牌組 144**：萬/筒/條 各 1–9 × 4 = 108；風牌 東南西北 × 4 = 16；
箭牌 中發白 × 4 = 12；花牌 春夏秋冬梅蘭竹菊 × 1 = 8。

**流程**：擲骰決莊 → 各摸 16 張（莊 17）→ 補花（順時針，補到沒花）→ 莊出牌 →
逆時針摸打 → 胡 / 流局。

**胡牌型**：5 面子 + 1 對將。標準解法＝先抽將，三門數牌各自遞迴拆刻子/順子 + 記憶化；
字牌只能刻子。特殊型另走（十三么、八仙過海）。

**優先權仲裁 ★ 最容易寫錯**：胡 > 槓/碰 > 吃，且只有下家能吃。
必須做成明確的「等待反應」狀態機，不能用 if 串。

### v1 台數表（2026-09-01 使用者拍板「最簡單好算」，已定案）

**設計原則（這條決定了整張表長什麼樣）**
> 凡是需要知道「你聽的是什麼形狀」的台，一律不採。

被這條切掉的：**平胡、獨聽、單吊、邊張、嵌張**。它們是整張表裡唯一逼你另寫一套
「回推聽牌型」邏輯的項目，而且各地定義最不一致（平胡要不要門清？將能不能是字牌？）。
剩下每一項都只看兩種東西：**①拆好的牌型　②怎麼胡的旗標**。

**七對子不採** —— 不是取捨：七對子是 13 張玩法，16 張湊不出來（對應的是八對子，台灣極少打）。
⇒ 台灣 16 張的胡牌型**只有一種**：5 面子 + 1 對將。判定沒有任何特例分支。

| 台 | 項目 | 判定看什麼 |
|---|---|---|
| 1 | 自摸 / 門清 / 不求人（門清+自摸） | 旗標 |
| 1 | 槓上開花 / 海底撈月 / 河底撈魚 / 搶槓 | 旗標 |
| 1 | 花牌**每張** | 旗標（8 張自然就是 8 台，「八仙過海」不另立規則） |
| 1 | 門風刻 / 圈風刻 / 中 / 發 / 白 **各** | 牌型 |
| 2 / 5 / 8 | 三暗刻 / 四暗刻 / 五暗刻 | 牌型（**唯一一條排除規則：取最高一項**） |
| 4 | 碰碰胡 / 混一色 / 小三元 / 混老頭 | 牌型 |
| 8 | 清一色 / 大三元 / 小四喜 | 牌型 |
| 16 | 字一色 / 大四喜 / 清老頭 / 地胡 | 牌型 or 旗標 |
| 24 | 天胡 | 旗標 |
| 2N+1 | 莊家連 N 拉 N（N=0 也是 1 台） | 旗標 |

**為什麼幾乎不用排除規則**：清一色／混一色／字一色、大三元／小三元、大四喜／小四喜、
混老頭／清老頭／字一色，全部**由定義互斥**（混老頭定義成「同時有么九數牌與字牌」，
就自動跟清老頭、字一色分開了）。三元刻與大小三元則是**刻意都算**——
這是台灣多數牌桌的打法，而且少一條規則就少一個 bug。

**食胡補成的刻子算明刻**（影響暗刻數）：榮和時，胡的那張所在的刻子標為明。

> 隨時可以加回來：`score.js` 就是一張表 + 一組 golden cases，加一項 = 加一行 + 加一個 case。

## 八、驗收（沿紙牌桌慣例）

- `node scripts/smoke.mjs` — 純邏輯。守恆、種子重現、亂走 bot 500 局、
  胡牌型 golden、台數 golden。**項數以跑出來的為準，文件不寫死**。
- `node scripts/verify-ui.mjs` — 真瀏覽器；`LIVE_URL=` 可驗線上；含 iPhone 觸控體檢。
